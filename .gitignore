node_modules/
.astro/
dist/
.DS_Store
*.log

# Variables de entorno: nunca al repositorio.
.env
.env.*
!.env.example

# Certificados locales para redes con inspección TLS (no son del proyecto).
certs/

# Material interno de campaña (presupuesto, grabaciones, fotos sin editar).
# Este repositorio es público: se deja fuera a propósito.
Campaña de marketing político _ Lupe Navarrete/

# Documentos internos de planificacion (huecos de cumplimiento legal, grafia
# de la boleta, inventario del material privado). Se quedan en local.
INVENTARIO-ASSETS.md
CONTENIDO-PENDIENTE.md
src/_legacy-home/

# Videos de origen sin optimizar (218 MB). Solo suben los convertidos.
videos/
